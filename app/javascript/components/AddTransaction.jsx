import React from "react";

// Work in progress
class AddTransaction extends React.Component {
  render() {
    return (
      <span>Form to add a transaction.</span>
    );
  }

}
export default AddTransaction;
